# Anthropic API 키 (필수)
ANTHROPIC_API_KEY=sk-ant-...

# 분석에 쓸 모델. 기본값은 claude-opus-5.
# 조항 수십 개를 배치로 호출하므로 비용이 부담되면 claude-sonnet-5 로 낮춰도 됨.
MODEL=claude-opus-5

# 한 번의 LLM 호출에 묶어 보낼 조항 수 (비용/속도 트레이드오프)
CLAUSES_PER_CALL=10

# 업로드 최대 크기 (MB)
MAX_UPLOAD_MB=20
